node_modules/
dist/
.env
.env.local

# Uploaded resumes and the local datastore — real candidate data, never commit.
server/uploads/*
!server/uploads/.gitkeep
server/data/db.json

.DS_Store
*.log

# Tesseract language data, downloaded on first OCR run.
server/*.traineddata

# TypeScript build output
server/dist/
*.tsbuildinfo

# Credential file patterns. .env is already covered above; these catch the
# variants people reach for when they are moving fast.
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
credentials.json
service-account*.json
*-secrets.y*ml

# Security log, if SECURITY_LOG_FILE points inside the repo.
*.security.log
# Sample resume data — contains real names, don't publish.
samples/